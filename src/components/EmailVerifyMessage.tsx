import React from "react";

const EmailVerifyMessage = ({
  message,
  status,
}: {
  message: string;
  status: string;
}) => {
  return <div>EmailVerifyMessage</div>;
};

export default EmailVerifyMessage;
